# Relay testimonial — Jessie, GreenRoots

**Status:** unpublished. Held as a source for a future testimonials section on
the Relay product page (`/products`). Do not publish without asking.

**Received:** 2026-08-28, by text, in reply to "Can you text me a testimonial of
Relay?"

**Attribution:** Jessie, GreenRoots. No role or title captured — get one before
publishing.

## Quote (verbatim)

> Relay has been a total game-changer for our workflow! I love how easy it is to
> communicate with customers and send appointment reminders. The automation
> features help everything run more smoothly, keep customers in the loop, and
> make my job a whole lot easier. It's one of those tools you quickly wonder how
> you ever worked without it!

## Notes

- The accompanying screenshot is not needed, so there is no portrait asset. A
  testimonials section on `/products` should be text-only or use a logo avatar —
  `.testimonial23_logo-avatar` on the home page is the existing pattern for the
  latter.
- Do **not** add this to `src/content/testimonials/`. That collection feeds
  `TestimonialSlider` on the home page, so a file there publishes the quote
  immediately.
- `/products` currently has no social proof at all; this would be the first.
  Built on the `mk-` kit, a single quote suits `.mk-callout`, and several would
  suit `.mk-card` on a sand band. See `/ui-kit` for both.
